import { createItemConfig } from "../config";

export const twigConfig = createItemConfig({
  id: "twig",
  name: "Twig",
  description: "A small, flexible branch. Perfect for kindling or simple crafts.",
  iconPath: "/assets/twig.svg",
  maxStackSize: 5,
  weight: 0.05,
  rarity: "common"
});
